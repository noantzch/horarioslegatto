import React from 'react'

const Signlayout = ( children: React.ReactNode ) => {
    return (
      <div>
        <p>HOla</p>
        <div>{children}</div>
        <p>Chau</p>
      </div>
    );
  };

export default Signlayout